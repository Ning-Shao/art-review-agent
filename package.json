{
  "name": "ai-art-review-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node scripts/check-js.mjs",
    "test": "npm run check && npm run build && node tests/smoke.spec.js"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
